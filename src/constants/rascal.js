export const DEFAULT_QUEUE_OPTIONS = {
  assert: true,
  options: {
    durable: true,
  },
}

export const DEFAULT_EXCHANGE_OPTIONS = {
  assert: true,
  options: {
    durable: true,
  },
}

export const DEFAULT_QUEUE_ERROR_OPTIONS = {
  assert: true,
  options: {
    durable: true,
  },
}
